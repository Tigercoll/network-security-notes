---
title: Day055：渗透测试方法论 - 复测与修复跟踪、沟通要点
tags:
  - 网络
  - 安全
  - 学习计划
categories:
  - 网络安全
abbrlink: 476dc072
date: 2026-02-17 00:00:00
updated: 2026-02-17 00:00:00

---
# Day055：渗透测试方法论 - 复测与修复跟踪、沟通要点

- 日期：2026-02-17
- 周次：第8周

## 学习目标

今天你将掌握渗透测试复测与修复跟踪的核心知识：

- **理解复测流程**：掌握漏洞修复后的验证方法和流程
- **管理修复跟踪**：能建立有效的漏洞修复跟踪机制
- **跨团队沟通**：掌握与开发、运维、安全团队的沟通技巧
- **版本控制关联**：能将漏洞与代码版本和部署版本关联
- **形成闭环**：能完成从发现到修复到验证的完整闭环

---

<!--more-->

## 学习内容

### 1️⃣ 复测流程设计

#### 1.1 复测计划制定

```python
#!/usr/bin/env python3
"""
渗透测试复测管理系统
"""
from __future__ import annotations
from dataclasses import dataclass, field
from datetime import datetime
from enum import Enum
from typing import Optional
from pathlib import Path
import json


class RetestStatus(Enum):
    """复测状态"""
    PENDING = "待复测"
    IN_PROGRESS = "复测中"
    PASSED = "已通过"
    FAILED = "未通过"
    PARTIAL = "部分通过"
    DEFERRED = "已延期"


class RemediationStatus(Enum):
    """修复状态"""
    OPEN = "待修复"
    IN_PROGRESS = "修复中"
    VERIFIED = "已验证"
    ACCEPTED = "已接受风险"
    FALSE_POSITIVE = "误报"


@dataclass
class RetestRecord:
    """复测记录"""
    finding_id: str
    vulnerability_type: str
    original_severity: str
    original_cvss: float
    affected_asset: str
    remediation_description: str
    retest_date: datetime
    retester: str
    status: RetestStatus
    test_method: str
    test_result: str
    evidence_files: list[str] = field(default_factory=list)
    notes: str = ""


@dataclass
class RetestPlan:
    """复测计划"""
    plan_id: str
    engagement_id: str
    created_date: datetime
    due_date: datetime
    records: list[RetestRecord] = field(default_factory=list)
    total_findings: int = 0
    completed_findings: int = 0
    passed_findings: int = 0
    failed_findings: int = 0


class RetestManager:
    """复测管理器"""
    
    def __init__(self):
        self.retest_plans: list[RetestPlan] = []
    
    def create_retest_plan(
        self,
        engagement_id: str,
        due_date: datetime,
        findings: list[dict]
    ) -> RetestPlan:
        """创建复测计划"""
        plan = RetestPlan(
            plan_id=f"RET-{datetime.now().strftime('%Y%m%d')}-001",
            engagement_id=engagement_id,
            created_date=datetime.now(),
            due_date=due_date
        )
        
        for finding in findings:
            record = RetestRecord(
                finding_id=finding['id'],
                vulnerability_type=finding['type'],
                original_severity=finding['severity'],
                original_cvss=finding['cvss'],
                affected_asset=finding['asset'],
                remediation_description=finding.get('remediation', ''),
                retest_date=datetime.now(),
                retester="",
                status=RetestStatus.PENDING,
                test_method="",
                test_result=""
            )
            plan.records.append(record)
        
        plan.total_findings = len(plan.records)
        self.retest_plans.append(plan)
        
        return plan
    
    def execute_retest(
        self,
        plan_id: str,
        finding_id: str,
        tester: str,
        test_method: str,
        test_result: str,
        passed: bool,
        notes: str = ""
    ) -> None:
        """执行复测"""
        for plan in self.retest_plans:
            if plan.plan_id == plan_id:
                for record in plan.records:
                    if record.finding_id == finding_id:
                        record.retester = tester
                        record.retest_date = datetime.now()
                        record.test_method = test_method
                        record.test_result = test_result
                        record.notes = notes
                        
                        if passed:
                            record.status = RetestStatus.PASSED
                            plan.passed_findings += 1
                        else:
                            record.status = RetestStatus.FAILED
                            plan.failed_findings += 1
                        
                        plan.completed_findings += 1
                        break
    
    def generate_retest_report(self, plan_id: str) -> str:
        """生成复测报告"""
        for plan in self.retest_plans:
            if plan.plan_id == plan_id:
                report = f"""
# 渗透测试复测报告

## 复测概览

- **计划ID**: {plan.plan_id}
- **项目ID**: {plan.engagement_id}
- **创建日期**: {plan.created_date.strftime('%Y-%m-%d')}
- **截止日期**: {plan.due_date.strftime('%Y-%m-%d')}

## 统计信息

| 指标 | 数量 |
|------|------|
| 总发现数 | {plan.total_findings} |
| 已复测 | {plan.completed_findings} |
| 通过 | {plan.passed_findings} |
| 未通过 | {plan.failed_findings} |
| 待复测 | {plan.total_findings - plan.completed_findings} |

## 复测详情

"""
                for record in plan.records:
                    report += f"""
### {record.finding_id}: {record.vulnerability_type}

- **原始严重性**: {record.original_severity} (CVSS: {record.original_cvss})
- **影响资产**: {record.affected_asset}
- **修复措施**: {record.remediation_description}
- **复测日期**: {record.retest_date.strftime('%Y-%m-%d %H:%M')}
- **复测人员**: {record.retester}
- **复测方法**: {record.test_method}
- **复测结果**: {record.test_result}
- **状态**: {record.status.value}

"""
                return report
        
        return "Plan not found"
    
    def export_plan_status(self, plan_id: str) -> dict:
        """导出计划状态"""
        for plan in self.retest_plans:
            if plan.plan_id == plan_id:
                return {
                    "plan_id": plan.plan_id,
                    "engagement_id": plan.engagement_id,
                    "total_findings": plan.total_findings,
                    "completed": plan.completed_findings,
                    "passed": plan.passed_findings,
                    "failed": plan.failed_findings,
                    "progress": f"{(plan.completed_findings / plan.total_findings * 100):.1f}%" 
                    if plan.total_findings > 0 else "0%",
                    "status": "In Progress" if plan.completed_findings < plan.total_findings else "Completed"
                }
        return {}
```

#### 1.2 自动化复测脚本

```python
#!/usr/bin/env python3
"""
自动化漏洞复测脚本
"""
from __future__ import annotations
import subprocess
import socket
import requests
from dataclasses import dataclass
from typing import Optional
from datetime import datetime


@dataclass
class RetestResult:
    """复测结果"""
    vulnerability_id: str
    vulnerability_type: str
    target: str
    port: Optional[int]
    test_date: datetime
    test_method: str
    passed: bool
    output: str
    remediation_verified: bool


class AutomatedRetester:
    """自动化复测器"""
    
    def retest_sql_injection(self, url: str, parameter: str) -> RetestResult:
        """复测 SQL 注入"""
        # 尝试注入恶意 payload
        test_payload = "' OR '1'='1"
        
        try:
            response = requests.get(
                url,
                params={parameter: test_payload},
                timeout=10
            )
            
            # 检查是否有 SQL 错误或异常行为
            sql_errors = [
                "You have an error in your SQL syntax",
                "Warning: mysql",
                "ORA-",
                "SQLSyntaxError"
            ]
            
            passed = not any(error.lower() in response.text.lower() for error in sql_errors)
            
            return RetestResult(
                vulnerability_id="VULN-SQL-001",
                vulnerability_type="SQL Injection",
                target=url,
                port=80,
                test_date=datetime.now(),
                test_method="Payload injection test",
                passed=passed,
                output=f"Response status: {response.status_code}, Length: {len(response.text)}",
                remediation_verified=passed
            )
            
        except Exception as e:
            return RetestResult(
                vulnerability_id="VULN-SQL-001",
                vulnerability_type="SQL Injection",
                target=url,
                port=80,
                test_date=datetime.now(),
                test_method="Payload injection test",
                passed=False,
                output=f"Error: {str(e)}",
                remediation_verified=False
            )
    
    def retest_xss(self, url: str, parameter: str) -> RetestResult:
        """复测 XSS"""
        test_payload = "<script>alert('XSS')</script>"
        
        try:
            response = requests.get(
                url,
                params={parameter: test_payload},
                timeout=10
            )
            
            # 检查 payload 是否被过滤或编码
            passed = test_payload not in response.text
            
            return RetestResult(
                vulnerability_id="VULN-XSS-001",
                vulnerability_type="XSS",
                target=url,
                port=80,
                test_date=datetime.now(),
                test_method="Payload reflection test",
                passed=passed,
                output=f"Response contains payload: {not passed}",
                remediation_verified=passed
            )
            
        except Exception as e:
            return RetestResult(
                vulnerability_id="VULN-XSS-001",
                vulnerability_type="XSS",
                target=url,
                port=80,
                test_date=datetime.now(),
                test_method="Payload reflection test",
                passed=False,
                output=f"Error: {str(e)}",
                remediation_verified=False
            )
    
    def retest_smb_vulnerability(self, target: str, port: int = 445) -> RetestResult:
        """复测 SMB 漏洞 (如 MS17-010)"""
        try:
            # 创建 socket 连接
            sock = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
            sock.settimeout(5)
            sock.connect((target, port))
            
            # 发送 SMB negotiate 请求
            negotiate_request = b'\x00\x00\x00\x90\xff\x53\x4d\x42\x72\x00\x00\x00\x00\x18\x53\xc8\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00'
            sock.send(negotiate_request)
            
            response = sock.recv(1024)
            sock.close()
            
            # 检查是否仍然支持易受攻击的 SMB 版本
            # 漏洞已修复的系统不会返回特定的响应
            vulnerable = b'\x02\x02' in response  # SMB 1.0 响应
            
            return RetestResult(
                vulnerability_id="VULN-SMB-001",
                vulnerability_type="SMB Remote Code Execution",
                target=target,
                port=port,
                test_date=datetime.now(),
                test_method="SMB protocol check",
                passed=not vulnerable,
                output=f"SMB v1 supported: {vulnerable}",
                remediation_verified=not vulnerable
            )
            
        except Exception as e:
            return RetestResult(
                vulnerability_id="VULN-SMB-001",
                vulnerability_type="SMB Remote Code Execution",
                target=target,
                port=port,
                test_date=datetime.now(),
                test_method="SMB protocol check",
                passed=False,
                output=f"Connection error: {str(e)}",
                remediation_verified=False
            )
    
    def retest_weak_ssl(self, target: str, port: int) -> RetestResult:
        """复测弱 SSL/TLS 配置"""
        import ssl
        
        try:
            # 创建 SSL 上下文
            context = ssl.create_default_context()
            context.check_hostname = False
            context.verify_mode = ssl.CERT_NONE
            
            # 尝试连接
            with socket.create_connection((target, port), timeout=5) as sock:
                with context.wrap_socket(sock, server_hostname=target) as ssock:
                    cert = ssock.getpeercert()
                    version = ssock.version()
            
            # 检查是否使用弱加密套件
            # 简化的检查：TLS 1.2 以下版本视为弱配置
            passed = version in ['TLSv1.2', 'TLSv1.3']
            
            return RetestResult(
                vulnerability_id="VULN-SSL-001",
                vulnerability_type="Weak SSL/TLS Configuration",
                target=target,
                port=port,
                test_date=datetime.now(),
                test_method="TLS version check",
                passed=passed,
                output=f"TLS Version: {version}",
                remediation_verified=passed
            )
            
        except Exception as e:
            return RetestResult(
                vulnerability_id="VULN-SSL-001",
                vulnerability_type="Weak SSL/TLS Configuration",
                target=target,
                port=port,
                test_date=datetime.now(),
                test_method="TLS version check",
                passed=False,
                output=f"Error: {str(e)}",
                remediation_verified=False
            )
```

---

### 2️⃣ 修复跟踪系统

#### 2.1 修复跟踪数据库

```python
#!/usr/bin/env python3
"""
漏洞修复跟踪系统
"""
from __future__ import annotations
from dataclasses import dataclass, field
from datetime import datetime
from enum import Enum
from typing import Optional
import json


class Priority(Enum):
    """优先级"""
    P1_CRITICAL = "P1-Critical"
    P2_HIGH = "P2-High"
    P3_MEDIUM = "P3-Medium"
    P4_LOW = "P4-Low"


class Status(Enum):
    """状态"""
    OPEN = "Open"
    IN_PROGRESS = "In Progress"
    FIXED = "Fixed"
    VERIFIED = "Verified"
    CLOSED = "Closed"
    ACCEPTED = "Risk Accepted"


@dataclass
class RemediationTicket:
    """修复工单"""
    ticket_id: str
    finding_id: str
    title: str
    description: str
    severity: str
    priority: Priority
    status: Status
    assignee: str
    created_date: datetime
    due_date: datetime
    closed_date: Optional[datetime] = None
    jira_ticket: Optional[str] = None
    github_issue: Optional[str] = None
    commits: list[str] = field(default_factory=list)
    deployment_versions: list[str] = field(default_factory=list)
    notes: list[str] = field(default_factory=list)
    dependencies: list[str] = field(default_factory=list)


@dataclass
class RemediationProgress:
    """修复进度"""
    total_open: int = 0
    total_in_progress: int = 0
    total_fixed: int = 0
    total_verified: int = 0
    total_closed: int = 0
    overdue_count: int = 0
    by_priority: dict = field(default_factory=dict)


class RemediationTracker:
    """修复跟踪器"""
    
    def __init__(self):
        self.tickets: list[RemediationTicket] = []
    
    def create_ticket(
        self,
        finding_id: str,
        title: str,
        description: str,
        severity: str,
        assignee: str,
        due_date: datetime,
        jira_ticket: str = None
    ) -> RemediationTicket:
        """创建修复工单"""
        priority = self._determine_priority(severity)
        
        ticket = RemediationTicket(
            ticket_id=f"REM-{len(self.tickets) + 1:04d}",
            finding_id=finding_id,
            title=title,
            description=description,
            severity=severity,
            priority=priority,
            status=Status.OPEN,
            assignee=assignee,
            created_date=datetime.now(),
            due_date=due_date,
            jira_ticket=jira_ticket
        )
        
        self.tickets.append(ticket)
        return ticket
    
    def _determine_priority(self, severity: str) -> Priority:
        """确定优先级"""
        priority_map = {
            "Critical": Priority.P1_CRITICAL,
            "High": Priority.P2_HIGH,
            "Medium": Priority.P3_MEDIUM,
            "Low": Priority.P4_LOW
        }
        return priority_map.get(severity, Priority.P4_LOW)
    
    def update_status(self, ticket_id: str, status: Status) -> None:
        """更新状态"""
        for ticket in self.tickets:
            if ticket.ticket_id == ticket_id:
                ticket.status = status
                if status == Status.CLOSED:
                    ticket.closed_date = datetime.now()
                break
    
    def add_commit(self, ticket_id: str, commit_hash: str) -> None:
        """添加提交记录"""
        for ticket in self.tickets:
            if ticket.ticket_id == ticket_id:
                ticket.commits.append(commit_hash)
                ticket.status = Status.IN_PROGRESS
                break
    
    def add_deployment(self, ticket_id: str, version: str) -> None:
        """添加部署版本"""
        for ticket in self.tickets:
            if ticket.ticket_id == ticket_id:
                ticket.deployment_versions.append(version)
                ticket.status = Status.FIXED
                break
    
    def verify_fix(self, ticket_id: str, verified: bool) -> None:
        """验证修复"""
        for ticket in self.tickets:
            if ticket.ticket_id == ticket_id:
                if verified:
                    ticket.status = Status.VERIFIED
                else:
                    ticket.status = Status.IN_PROGRESS
                break
    
    def generate_progress_report(self) -> RemediationProgress:
        """生成进度报告"""
        progress = RemediationProgress()
        
        for ticket in self.tickets:
            if ticket.status == Status.OPEN:
                progress.total_open += 1
            elif ticket.status == Status.IN_PROGRESS:
                progress.total_in_progress += 1
            elif ticket.status == Status.FIXED:
                progress.total_fixed += 1
            elif ticket.status == Status.VERIFIED:
                progress.total_verified += 1
            elif ticket.status == Status.CLOSED:
                progress.total_closed += 1
            
            # 检查是否逾期
            if ticket.status not in [Status.CLOSED, Status.VERIFIED]:
                if ticket.due_date < datetime.now():
                    progress.overdue_count += 1
        
        # 按优先级统计
        for ticket in self.tickets:
            priority = ticket.priority.value
            if priority not in progress.by_priority:
                progress.by_priority[priority] = 0
            progress.by_priority[priority] += 1
        
        return progress
    
    def export_to_csv(self, filename: str) -> None:
        """导出到 CSV"""
        import csv
        
        with open(filename, 'w', newline='', encoding='utf-8') as f:
            writer = csv.writer(f)
            writer.writerow([
                'Ticket ID', 'Finding ID', 'Title', 'Severity', 
                'Priority', 'Status', 'Assignee', 'Created Date',
                'Due Date', 'JIRA Ticket', 'Commits'
            ])
            
            for ticket in self.tickets:
                writer.writerow([
                    ticket.ticket_id,
                    ticket.finding_id,
                    ticket.title,
                    ticket.severity,
                    ticket.priority.value,
                    ticket.status.value,
                    ticket.assignee,
                    ticket.created_date.strftime('%Y-%m-%d'),
                    ticket.due_date.strftime('%Y-%m-%d'),
                    ticket.jira_ticket or '',
                    ', '.join(ticket.commits)
                ])
```

#### 2.2 修复跟踪看板

```python
#!/usr/bin/env python3
"""
修复跟踪看板生成器
"""
from dataclasses import dataclass
from typing import list


@dataclass
class KanbanColumn:
    """看板列"""
    name: str
    tickets: list[dict]


class RemediationKanban:
    """修复跟踪看板"""
    
    def __init__(self):
        self.columns = {
            "待处理": [],
            "进行中": [],
            "待验证": [],
            "已完成": [],
            "已逾期": []
        }
    
    def add_ticket(self, ticket: dict) -> None:
        """添加工单到看板"""
        status = ticket.get('status', 'Open')
        
        if status == 'Open':
            self.columns["待处理"].append(ticket)
        elif status == 'In Progress':
            self.columns["进行中"].append(ticket)
        elif status == 'Fixed':
            self.columns["待验证"].append(ticket)
        elif status in ['Verified', 'Closed']:
            self.columns["已完成"].append(ticket)
        
        # 检查逾期
        if ticket.get('due_date') and ticket.get('due_date') < datetime.now():
            if status not in ['Verified', 'Closed']:
                self.columns["已逾期"].append(ticket)
    
    def generate_visual_report(self) -> str:
        """生成可视化报告"""
        report = """
# 漏洞修复跟踪看板

## 总体进度

| 状态 | 数量 | 占比 |
|------|------|------|
"""
        
        total = sum(len(col) for col in self.columns.values()) - len(self.columns["已逾期"])  # 排除逾期重复计算
        
        for column_name, tickets in self.columns.items():
            if column_name == "已逾期":
                continue
            
            count = len(tickets)
            percentage = (count / max(total, 1)) * 100
            
            report += f"| {column_name} | {count} | {percentage:.1f}% |\n"
        
        report += "\n## 详细看板\n\n"
        
        for column_name, tickets in self.columns.items():
            if tickets:
                report += f"### {column_name}\n\n"
                report += "| ID | 标题 | 严重性 | 负责人 | 截止日期 |\n"
                report += "|-----|------|--------|--------|----------|\n"
                
                for ticket in sorted(tickets, key=lambda x: x.get('severity', '')):
                    report += f"| {ticket.get('ticket_id', 'N/A')} | "
                    report += f"{ticket.get('title', 'N/A')[:30]} | "
                    report += f"{ticket.get('severity', 'N/A')} | "
                    report += f"{ticket.get('assignee', 'N/A')} | "
                    report += f"{ticket.get('due_date', 'N/A')} |\n"
                
                report += "\n"
        
        return report
```

---

### 3️⃣ 跨团队沟通

#### 3.1 沟通模板

```python
#!/usr/bin/env python3
"""
渗透测试沟通模板
"""
from __future__ import annotations
from dataclasses import dataclass
from datetime import datetime
from typing import Optional


@dataclass
class CommunicationRecord:
    """沟通记录"""
    record_id: str
    date: datetime
    from_person: str
    to_person: str
    subject: str
    method: str  # Email, Meeting, Call, Chat
    content: str
    outcome: str
    action_items: list[str] = None


class CommunicationManager:
    """沟通管理器"""
    
    def __init__(self):
        self.records: list[CommunicationRecord] = []
    
    def send_vulnerability_notification(
        self,
        finding_id: str,
        severity: str,
        to_email: str,
        developer_name: str,
        remediation_link: str
    ) -> CommunicationRecord:
        """发送漏洞通知"""
        template = f"""
安全漏洞通知

尊敬的 {developer_name}：

我们发现了一个需要紧急处理的安全漏洞：

漏洞 ID: {finding_id}
严重性: {severity}

漏洞详情和修复建议请查看：
{remediation_link}

请在 24 小时内确认并开始修复。

如有任何问题，请及时联系安全团队。

安全团队
{datetime.now().strftime('%Y-%m-%d')}
"""
        
        record = CommunicationRecord(
            record_id=f"COM-{len(self.records) + 1:04d}",
            date=datetime.now(),
            from_person="安全团队",
            to_person=to_email,
            subject=f"[紧急] 安全漏洞通知 - {finding_id}",
            method="Email",
            content=template,
            outcome="待确认",
            action_items=[f"开发者确认收到通知", f"开始修复工作"]
        )
        
        self.records.append(record)
        return record
    
    def schedule_remediation_meeting(
        self,
        topic: str,
        attendees: list[str],
        scheduled_date: datetime,
        agenda: list[str]
    ) -> CommunicationRecord:
        """安排修复会议"""
        agenda_text = "\n".join([f"{i+1}. {item}" for i, item in enumerate(agenda)])
        
        template = f"""
修复讨论会议

主题: {topic}
时间: {scheduled_date.strftime('%Y-%m-%d %H:%M')}
参与人: {', '.join(attendees)}

议程:
{agenda_text}

请准时参加。
"""
        
        record = CommunicationRecord(
            record_id=f"COM-{len(self.records) + 1:04d}",
            date=datetime.now(),
            from_person="安全团队",
            to_person=", ".join(attendees),
            subject=f"修复讨论会议 - {topic}",
            method="Meeting",
            content=template,
            outcome="待定",
            action_items=agenda
        )
        
        self.records.append(record)
        return record
    
    def generate_weekly_summary(
        self,
        week_start: datetime,
        new_findings: int,
        fixed_findings: int,
        pending_findings: int,
        critical_findings: list[str]
    ) -> str:
        """生成每周总结"""
        summary = f"""
# 安全漏洞修复周报

## 周报期间
{week_start.strftime('%Y-%m-%d')} 至 {(week_start + datetime.timedelta(days=7)).strftime('%Y-%m-%d')}

## 总体统计

| 指标 | 数量 |
|------|------|
| 新增发现 | {new_findings} |
| 已修复 | {fixed_findings} |
| 待修复 | {pending_findings} |
| 修复率 | {fixed_findings / max(new_findings, 1) * 100:.1f}% |

## Critical 漏洞处理情况
"""
        
        for finding in critical_findings:
            summary += f"- {finding}\n"
        
        summary += f"""
## 下周计划

1. 继续跟进 Critical 漏洞修复
2. 对 Medium 级别漏洞进行修复
3. 进行复测验证

## 需要关注的团队

请各团队负责人关注本团队负责的漏洞修复进度。
"""
        
        return summary
```

---

### 4️⃣ 版本与部署关联

#### 4.1 版本追踪系统

```python
#!/usr/bin/env python3
"""
漏洞与版本关联追踪
"""
from __future__ import annotations
from dataclasses import dataclass
from datetime import datetime
from typing import Optional
from pathlib import Path


@dataclass
class VulnerabilityFix:
    """漏洞修复记录"""
    vulnerability_id: str
    cve_id: Optional[str]
    description: str
    affected_versions: list[str]
    fixed_in_version: str
    commit_hash: str
    commit_date: datetime
    commit_author: str
    pull_request: Optional[str]
    merge_request: Optional[str]


@dataclass
class DeploymentRecord:
    """部署记录"""
    deployment_id: str
    version: str
    environment: str  # Dev, Staging, Production
    deployed_by: str
    deployed_date: datetime
    deployment_method: str  # CI/CD, Manual, Blue-Green
    previous_version: str
    status: str  # Success, Failed, Rollback


class VersionTracker:
    """版本追踪器"""
    
    def __init__(self):
        self.fixes: list[VulnerabilityFix] = []
        self.deployments: list[DeploymentRecord] = []
    
    def record_fix(
        self,
        vulnerability_id: str,
        cve_id: str,
        description: str,
        affected_versions: list[str],
        fixed_in_version: str,
        commit_hash: str,
        commit_date: datetime,
        commit_author: str,
        pull_request: str = None
    ) -> VulnerabilityFix:
        """记录漏洞修复"""
        fix = VulnerabilityFix(
            vulnerability_id=vulnerability_id,
            cve_id=cve_id,
            description=description,
            affected_versions=affected_versions,
            fixed_in_version=fixed_in_version,
            commit_hash=commit_hash,
            commit_date=commit_date,
            commit_author=commit_author,
            pull_request=pull_request,
            merge_request=None
        )
        
        self.fixes.append(fix)
        return fix
    
    def record_deployment(
        self,
        version: str,
        environment: str,
        deployed_by: str,
        deployment_method: str,
        previous_version: str
    ) -> DeploymentRecord:
        """记录部署"""
        deployment = DeploymentRecord(
            deployment_id=f"DEP-{len(self.deployments) + 1:04d}",
            version=version,
            environment=environment,
            deployed_by=deployed_by,
            deployed_date=datetime.now(),
            deployment_method=deployment_method,
            previous_version=previous_version,
            status="Success"
        )
        
        self.deployments.append(deployment)
        return deployment
    
    def get_fixed_vulnerabilities_for_version(self, version: str) -> list[VulnerabilityFix]:
        """获取某版本修复的漏洞"""
        return [f for f in self.fixes if f.fixed_in_version == version]
    
    def get_vulnerabilities_fixed_in_deployment(self, deployment_id: str) -> list[VulnerabilityFix]:
        """获取某部署修复的漏洞"""
        for deployment in self.deployments:
            if deployment.deployment_id == deployment_id:
                return self.get_fixed_vulnerabilities_for_version(deployment.version)
        return []
    
    def generate_version_report(self, version: str) -> str:
        """生成版本报告"""
        fixes = self.get_fixed_vulnerabilities_for_version(version)
        
        report = f"""
# 版本 {version} 安全修复报告

## 修复的漏洞

| 漏洞 ID | CVE | 描述 | 修复提交 |
|---------|-----|------|----------|
"""
        
        for fix in fixes:
            report += f"| {fix.vulnerability_id} | {fix.cve_id or 'N/A'} | "
            report += f"{fix.description[:50]}... | "
            report += f"[{fix.commit_hash[:7]}](https://github.com/repo/commit/{fix.commit_hash}) |\n"
        
        # 查找部署信息
        deployments = [d for d in self.deployments if d.version == version]
        
        if deployments:
            report += f"\n## 部署信息\n\n"
            for dep in deployments:
                report += f"- 环境: {dep.environment}\n"
                report += f"- 部署时间: {dep.deployed_date.strftime('%Y-%m-%d %H:%M')}\n"
                report += f"- 部署人: {dep.deployed_by}\n"
                report += f"- 部署方式: {dep.deployment_method}\n\n"
        
        return report
    
    def check_vulnerability_status(self, vulnerability_id: str) -> dict:
        """检查漏洞状态"""
        for fix in self.fixes:
            if fix.vulnerability_id == vulnerability_id:
                # 查找最新部署
                deployments = sorted(
                    [d for d in self.deployments if d.version == fix.fixed_in_version],
                    key=lambda x: x.deployed_date,
                    reverse=True
                )
                
                production_deployed = any(
                    d.environment == "Production" 
                    for d in deployments
                )
                
                return {
                    "vulnerability_id": vulnerability_id,
                    "fixed_in_version": fix.fixed_in_version,
                    "commit": fix.commit_hash,
                    "deployed": production_deployed,
                    "production_deployment": deployments[0] if deployments else None
                }
        
        return {"vulnerability_id": vulnerability_id, "status": "Not Found"}
```

---

## 实践任务（合法授权范围内）

> **注意**：以下任务请在你自己的测试环境、虚拟机或授权靶场中执行。

---

### 任务 1（必做）：创建修复跟踪系统

**目标**：建立漏洞修复跟踪机制。

**步骤**：

1. **创建跟踪数据库**

```python
#!/usr/bin/env python3

# 创建跟踪器
tracker = RemediationTracker()

# 添加修复工单
ticket1 = tracker.create_ticket(
    finding_id="VULN-001",
    title="修复 SQL 注入漏洞",
    description="在用户登录接口存在 SQL 注入",
    severity="Critical",
    assignee="张三",
    due_date=datetime.now() + datetime.timedelta(days=3),
    jira_ticket="SEC-1234"
)

# 更新状态
tracker.update_status(ticket1.ticket_id, Status.IN_PROGRESS)
tracker.add_commit(ticket1.ticket_id, "abc123def")

# 生成进度报告
progress = tracker.generate_progress_report()
print(f"总工单: {progress.total_open + progress.total_in_progress}")
print(f"逾期: {progress.overdue_count}")
```

2. **导出跟踪报告**

```python
# 导出到 CSV
tracker.export_to_csv("remediation_tracker.csv")

# 生成看板报告
kanban = RemediationKanban()
for ticket in tracker.tickets:
    kanban.add_ticket({
        'ticket_id': ticket.ticket_id,
        'title': ticket.title,
        'severity': ticket.severity,
        'assignee': ticket.assignee,
        'due_date': ticket.due_date.strftime('%Y-%m-%d'),
        'status': ticket.status.value
    })

print(kanban.generate_visual_report())
```

---

### 任务 2（必做）：执行复测

**目标**：对已修复的漏洞进行复测。

**步骤**：

```python
#!/usr/bin/env python3

# 创建自动化复测器
retester = AutomatedRetester()

# 复测 SQL 注入漏洞
result = retester.retest_sql_injection(
    url="http://10.0.0.10/login",
    parameter="username"
)

print(f"漏洞类型: {result.vulnerability_type}")
print(f"测试时间: {result.test_date}")
print(f"是否通过: {result.passed}")
print(f"测试输出: {result.output}")

# 复测 XSS 漏洞
result = retester.retest_xss(
    url="http://10.0.0.10/search",
    parameter="q"
)

print(f"\n漏洞类型: {result.vulnerability_type}")
print(f"是否通过: {result.passed}")
```

---

### 任务 3（必做）：编写沟通报告

**目标**：生成修复进度周报。

**步骤**：

```python
#!/usr/bin/env python3

# 创建沟通管理器
comm_manager = CommunicationManager()

# 发送漏洞通知
notification = comm_manager.send_vulnerability_notification(
    finding_id="VULN-001",
    severity="Critical",
    to_email="developer@company.com",
    developer_name="张三",
    remediation_link="https://security.company.com/remediation/VULN-001"
)

# 生成周报
weekly_summary = comm_manager.generate_weekly_summary(
    week_start=datetime.now(),
    new_findings=5,
    fixed_findings=3,
    pending_findings=7,
    critical_findings=["VULN-001", "VULN-002"]
)

print(weekly_summary)
```

---

## 巩固练习（题与复盘）

---

### 练习 1：复测偏差控制

**问题**：如何确保复测结果的准确性和一致性？

**思路提示**：

| 偏差来源 | 控制措施 |
|----------|----------|
| **环境差异** | 使用相同版本的测试环境 |
| **数据差异** | 使用标准化的测试数据集 |
| **工具差异** | 统一测试工具版本和配置 |
| **人为差异** | 制定详细的测试用例 |
| **时间差异** | 在相同的时间窗口内测试 |

---

### 练习 2：沟通效果评估

**问题**：如何评估和优化与开发团队的沟通效果？

**示例答案**：

```python
# 沟通效果评估指标
COMMUNICATION_METRICS = {
    "响应时间": {
        "Critical": "< 1小时",
        "High": "< 4小时",
        "Medium": "< 24小时",
        "Low": "< 48小时"
    },
    "修复周期": {
        "Critical": "24-72小时",
        "High": "1周",
        "Medium": "2周",
        "Low": "1个月"
    },
    "沟通质量": {
        "清晰度": "漏洞描述是否易于理解",
        "可操作性": "修复建议是否具体可行",
        "及时性": "通知和跟进是否及时",
        "专业性": "沟通方式是否专业"
    }
}

# 持续改进措施
IMPROVEMENT_ACTIONS = [
    "定期举办安全培训",
    "建立安全知识库",
    "优化漏洞描述模板",
    "引入自动化复测",
    "建立修复奖励机制"
]
```

---

### 练习 3：修复闭环管理

**问题**：如何确保漏洞从发现到修复的完整闭环？

**示例答案**：

```python
# 完整闭环流程
CLOSED_LOOP_PROCESS = {
    "阶段1: 发现": [
        "漏洞扫描或渗透测试",
        "漏洞验证和分类",
        "创建工单"
    ],
    "阶段2: 修复": [
        "分配给开发团队",
        "实施修复措施",
        "代码审查"
    ],
    "阶段3: 部署": [
        "部署到测试环境",
        "自动化测试验证",
        "部署到生产环境"
    ],
    "阶段4: 复测": [
        "安全团队复测",
        "验证修复有效性",
        "确认无回归问题"
    ],
    "阶段5: 关闭": [
        "更新漏洞状态",
        "更新文档和知识库",
        "总结经验教训"
    ]
}
```

---

## 评估标准（达成判定）

- ✅ 能制定复测计划并执行自动化复测
- ✅ 能建立有效的漏洞修复跟踪机制
- ✅ 能与开发团队进行有效沟通
- ✅ 能将漏洞修复与代码版本关联
- ✅ 能完成从发现到修复的完整闭环

---

## 学习成果达成情况（由学习者填写）

### 截图与证据

- [ ] 复测计划截图
- [ ] 修复跟踪看板截图
- [ ] 沟通记录截图
- [ ] 版本关联报告截图

### 关键命令与输出

**复测执行**：
```bash
$ python3 retester.py --url http://10.0.0.10 --vuln sql_injection
[+] 测试 SQL 注入漏洞修复
[+] 测试时间: 2024-01-25 14:30:00
[+] 测试结果: PASSED
[+] 输出: Response contains no SQL errors
```

**修复跟踪**：
```bash
$ python3 tracker.py --export csv
[+] 导出修复跟踪数据到: remediation_tracker.csv
[+] 总工单: 15
[+] 已修复: 8
[+] 逾期: 2
```

### 结论与反思

**我今天搞清楚了**：

- 复测流程的标准和方法
- 漏洞修复跟踪的重要性
- 跨团队沟通的技巧
- 版本管理与漏洞修复的关联

**我差点搞混的是**：

- 修复状态和复测状态的区别
- 工单系统和版本系统的对应关系
- 不同严重性的修复时间要求

**明天我要继续补的是**：

- 权限提升技术
- 内网横向移动
- 持久化技术

**本次学习耗时**：约 4 小时

**掌握程度自评**：

- [ ] 😕 理解了基本概念，但实践不熟练
- [ ] 🙂 完成了基础任务
- [ ] 😃 完成了所有任务并理解原理
- [ ] 🤩 额外开发了完整的修复跟踪系统


## 学习成果示例填写（可照抄）

> 可将"示例"内容替换为你自己的时间与截图文件名。

### 截图与证据（示例）

- 任务 1：`images/dayXXX_task1.png`

### 关键命令与输出（示例）

```
命令示例：
输出示例：
```

### 结论与反思（示例）

**我今天搞清楚了**：
- （示例）理解了核心概念

**我差点搞混的是**：
- （示例）某个易混淆点

**明天我要继续补的是**：
- （示例）下一步深入方向

**本次学习耗时**：约 2 小时

**掌握程度自评**：
- [x] 😃 完成了所有任务并理解原理
